{
  "name": "gdg-persistent-lottery",
  "version": "1.0.0",
  "private": true,
  "description": "Persistent high-capacity lottery wheel for Vercel",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "node --check server.js && node --check lib/lottery.js && node build.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "ISC",
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
